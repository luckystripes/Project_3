<!DOCTYPE html>
<meta charset="utf-8">
<title>Transform Transitions</title>
<style>
h1{
color: blue;
font-size: 50px;

}
 body {
    margin: 0;
    background-image: url("assets/cashier.jpg");
    position: absolute;
    left: 50px;
    top: 0px;
  }

rect {
    stroke: #fff;
    stroke-width: .1px;
  }
</style>
<body>
<h1>Linkder</h1>
    <script src="http://d3js.org/d3.v3.min.js"></script>
      <script>
      var w = 520,
          h = 520,
          z = 20,
          x = w / z,
          y = h / z;

      var svg = d3.select("body").append("svg")
          .attr("width", w)
          .attr("height", h);

      svg.selectAll("rect")
          .data(d3.range(x * y))
        .enter().append("rect")
          .attr("transform", translate)
          .attr("width", z)
          .attr("height", z)
          .style("fill", function(d) { return d3.hsl(d % x / x * 360, 1, Math.floor(d / x) / y); })
          .on("mouseover", mouseover);

      function translate(d) {
        return "translate(" + (d % x) * z + "," + Math.floor(d / x) * z + ")";
      }

      function mouseover(d) {
        this.parentNode.appendChild(this);

        d3.select(this)
            .style("pointer-events", "none")
          .transition()
                .delay(100)  //mouseover delay to the action

            .duration(1050) //how long it takes to rotate over to show position
            .attr("transform", "translate(480,480)scale(23)rotate(180)")
          .transition()
            .delay(4500)
            .attr("transform", "translate(240,240)scale(0)")
            .style("fill-opacity", 0)
            .remove();
      }
      </script>
</body>