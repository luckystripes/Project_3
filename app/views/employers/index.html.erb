<div>
<h1>You have listed <%= pluralize(@jobs.count, "job") %> on Lindker</h1>
    <% @jobs.each do |job| %>
		<h2><%= link_to job.title, jobs_path(id: job.id) %></h2>


	<% end %>
	
	<div>
	<button type="button">
		<%= link_to "+ New Job", new_job_path  %>
	</button>
	</div>
  </div>
</div>

<script>
  d3.json("/jobs.json", function(data){
  	console.log(data);
  	
  });
</script>